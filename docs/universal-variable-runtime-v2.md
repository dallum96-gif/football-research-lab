# Universal Variable Runtime V2

The universal runtime resolves canonical aliases and native source fields. Native fields are discovered against the requested season through the existing research-field query layer. The runtime does not duplicate source extraction or identity joins.